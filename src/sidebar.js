// Panel lateral: modo colocación, campo de nombre + guardar, lista de guardados
// con acciones (cargar/centrar, copiar enlace, renombrar, eliminar).
// Ver requisitos_wme_area_manager.md, sección 2 (puntos 1, 6, 7, 10).

export function initSidebar({ sdk, layer }) {
  // TODO (Fases 1-3): sdk.Sidebar.registerScriptTab() + construir el UI
  // (botón de colocación, campo de nombre, botón guardar, lista de guardados).
}
