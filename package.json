{
  "name": "wme-area-manager",
  "version": "0.11.0",
  "description": "Userscript de Tampermonkey para WME: traza rectángulos de área según el nivel del editor, con enlace al centro y guardado de rectángulos con nombre.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "test": "node src/self-check.js",
    "version": "node build.js && git add dist"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@turf/area": "^7.3.5",
    "@turf/destination": "^7.3.5",
    "@turf/helpers": "^7.3.5"
  }
}
